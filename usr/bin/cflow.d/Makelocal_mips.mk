#	@(#)Makelocal_mips.mk	4.1	ULTRIX	7/17/90
#
# rules for the include files (machine dependent)
# and the name of the cflow shell for install to use since it
# is also machine dependent.

CFLOWSHELL = cflow.mips
LINT = ../../../../lib/_mips.d/lint
MIP  = ../../../../lib/_mips.d/ccom
